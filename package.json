{
  "name": "bookmark-manager",
  "version": "1.0.0",
  "description": "A modern bookmark management system with Chrome extension sync",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "bookmarks",
    "chrome-extension",
    "cloudflare-pages",
    "d1-database",
    "turso"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "@libsql/client": "^0.4.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
